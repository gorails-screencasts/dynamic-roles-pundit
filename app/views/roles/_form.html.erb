<%= form_with(model: role, local: true) do |form| %>
  <% if role.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(role.errors.count, "error") %> prohibited this role from being saved:</h2>

      <ul>
      <% role.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-3">
    <%= form.label :name, class: 'form-label' %>
    <%= form.text_field :name, class: 'form-control' %>
  </div>

  <div class="mb-3">
    <%= form.label :permissions, class: 'form-label' %>
    <%= form.text_field :permissions, class: 'form-control' %>
  </div>

  <div class="mb-3">
    <% if role.persisted? %>
      <div class="float-right">
        <%= link_to 'Destroy', role, method: :delete, class: "text-danger", data: { confirm: 'Are you sure?' } %>
      </div>
    <% end %>

    <%= form.submit class: 'btn btn-primary' %>

    <% if role.persisted? %>
      <%= link_to "Cancel", role, class: "btn btn-link" %>
    <% else %>
      <%= link_to "Cancel", roles_path, class: "btn btn-link" %>
    <% end %>
  </div>
<% end %>
